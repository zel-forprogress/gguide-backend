spring:
  devtools:
    restart:
      enabled: true
      poll-interval: 2s
    livereload:
      enabled: true

logging:
  level:
    root: INFO
    org.springframework.web: DEBUG
    org.springframework.security: DEBUG
    person.hardy.gguide: DEBUG
